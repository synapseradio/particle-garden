name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.0-test)'
        required: true
        default: 'v0.0.1-test'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            artifact: particle-garden-macos.zip
          - os: windows-latest
            artifact: particle-garden-windows.zip
          - os: ubuntu-latest
            artifact: particle-garden-linux.tar.gz

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Nim
        uses: iffy/install-nim@v5
        with:
          version: binary:stable
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: nimble install -y --depsOnly --useSystemNim

      - name: Setup Nim paths
        run: nimble setup

      - name: Run tests
        run: nimble test

      - name: Build release
        run: nimble release

      # macOS: Create .app bundle
      - name: Package macOS
        if: matrix.os == 'macos-latest'
        run: |
          APP_NAME="Particle Garden"
          EXECUTABLE="particle_garden"
          BUNDLE_DIR="${APP_NAME}.app"

          mv main "${EXECUTABLE}"
          mkdir -p "${BUNDLE_DIR}/Contents/MacOS"
          mkdir -p "${BUNDLE_DIR}/Contents/Resources"
          cp "${EXECUTABLE}" "${BUNDLE_DIR}/Contents/MacOS/"

          cat >"${BUNDLE_DIR}/Contents/Info.plist" <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleName</key>
              <string>${APP_NAME}</string>
              <key>CFBundleDisplayName</key>
              <string>${APP_NAME}</string>
              <key>CFBundleIdentifier</key>
              <string>com.particlegarden.app</string>
              <key>CFBundleVersion</key>
              <string>${{ github.event.inputs.version || github.ref_name }}</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleExecutable</key>
              <string>${EXECUTABLE}</string>
              <key>LSMinimumSystemVersion</key>
              <string>10.13</string>
              <key>NSHighResolutionCapable</key>
              <true/>
          </dict>
          </plist>
          EOF

          # Use ditto instead of zip to preserve macOS bundle attributes
          ditto -c -k --sequesterRsrc --keepParent "${BUNDLE_DIR}" "${{ matrix.artifact }}"

          # Also create a simple zip with just the binary
          zip particle-garden-macos-bin.zip "${EXECUTABLE}"

      # Windows: Rename and zip
      - name: Package Windows
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          Rename-Item -Path "main.exe" -NewName "particle_garden.exe"
          Compress-Archive -Path "particle_garden.exe" -DestinationPath "${{ matrix.artifact }}"

      # Linux: Create tarball
      - name: Package Linux
        if: matrix.os == 'ubuntu-latest'
        run: |
          mv main particle_garden
          chmod +x particle_garden
          tar -czvf "${{ matrix.artifact }}" particle_garden

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

      - name: Upload macOS binary artifact
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: particle-garden-macos-bin.zip
          path: particle-garden-macos-bin.zip

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version || github.ref_name }}
          files: artifacts/**/*
          generate_release_notes: true
          draft: ${{ github.event_name == 'workflow_dispatch' }}
