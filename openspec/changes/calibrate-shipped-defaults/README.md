# calibrate-shipped-defaults

Replace three provisional constants with measured ones: the crowding ceiling, the SPH radius-fraction floor, and the force weather waypoints with their default speed. Also corrects two comments in `src/preset.nim` that state the shipped fraction default as something it is not.
