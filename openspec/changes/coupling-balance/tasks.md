## 0. Answers this group order waits on

- [x] 0.1 Record in `design.md` the user's answers to D2 (the radius-independent long-range unit), D3 (onset at the bottom of the self-attracting band) and D5 (pair viscosity 0.5 above the onset, reversed by the user in 0.2), and the lead's answers on the allotment's headroom (11.65 ms), `α = 5%`, and gates 5–7 in `just calibrate-balance`; revise the spec to match; verify `openspec validate coupling-balance --strict` passes
- [x] 0.2 Record in `design.md` D13 the user's choice of stiffness from the 128 000-particle trade (`K = 540` without viscosity, with its accepted costs), D14's corrected friction-0 cost, and the lead's answer on the allotment's headroom after the `w1-128k-150` correction (provisional, decided by task 4.5); revise the spec's pending clauses; verify `openspec validate coupling-balance --strict` passes

## 1. Measurement gates

- [ ] 1.1 Write the pressure oracle (the square law on `ρ` over `ρ_on = max(x_on · ρ̄, ρ_floor)`, magnitude saturated at `q_max`, no viscosity, one signed integer per component per pair, no `dt`) in `src/physics_core.nim`, the uniform crowd density, the lattice floor and a binned stepped oracle world with D16-law bodies in `src/balance_core.nim` (new), with the red suites "Pressure Past The Onset" and "The Force Law Is Untouched Below The Onset" in `tests/test_physics.nim` written first; verify both fail to compile before the oracle exists and pass after; verify "Pressure Past The Onset" turns red with a per-component saturation, a `fMul` factor and a Tait law, each locally, and that the bit-identity suite fails when the term is folded into the force law's `forceMultiplier * invDistance` product locally
- [ ] 1.2 Record a calibration seed set and a disjoint held-out seed set (at least 16 each) in `scratchpad/coupling-balance/`; on the calibration seeds settle worlds with no term at 100, 1 000, 16 000 and 128 000 particles and radii 10, 20, 50 and 150, species counts 1 (self-attracting), 4 and 12, both force models, each until its p99.9 changes less than its frame-to-frame spread; record the peaks in `ρ` and in `x`, the polynomial floor at the preset `repulsionEnd`, and the exponential model's floor where it has no rest spacing inside `R`; record `x_on` at the bottom of the self-attracting band with the D5 margin, and the densest mixed particles' trim at that onset
- [ ] 1.3 On the calibration seeds, at 128 000 particles and radius 50, one self-attracting species, `FRICTION_MIN`, no viscosity, measure L (late-window mean speed with the term over the same seed's without it) at the chosen `K = 540` and at `K = 1728`; record the runs, `s`, and `B_L = mean_cal(L at 540) + t · s · √(1/n_cal + 1/n_held)` at `α = 5%`, and that `K = 1728` exceeds `B_L`; at 16 000 particles, run the stacked hold at `K = 540` and record the after-over-fresh ratio's mean, `s`, and gate 6's `B_r` at `α = 5%`; at `K = 540` and shipped friction, 16 000 particles, bisect `ff_stable` as the largest frame factor, held fixed through a run, whose late-window mean speed per reference frame is no warmer than at frame factor 1 (one-sided paired `t`, `α = 5%`), and record it with its conditions (particle count, radius, species, onset, smoothing, retention, window, seeds); then run the jittered arms (frame factor drawn per frame uniformly from 8 to 16, and alternating 10 and 13, through the substep rule at the bisected `ff_stable`) and record that each is no warmer than frame factor 1, or return the trigger's hysteresis to the design if either is warmer
- [ ] 1.4 At the recorded `x_on`, `K` and `q_max`, run the 16 000-particle stacked hold on the calibration seeds and its stiffness-zero control bounded to 100 held frames; verify the held peak stays below the control's and record the after-release neighbour ratios; return the design if the held peak reaches the control's
- [ ] 1.5 Add the coarse word, the split and the term to `web/shaders/src/forces.wgsl` and `web/shaders/src/forces-sph.wgsl` behind `K = 0` and launch the app per CLAUDE.md with Browser MCP connected; read `physics=` from `[gpu-profile]` at `MAX_PARTICLES` on a settled world at the shipped radius with the coarse word bound and unbound, and with the term at `K = 0` and at the recorded `K`, and on a uniform world at two radii; record the per-neighbour cost, the word's cost and the term's added cost in `scratchpad/coupling-balance/`; compare the term's and word's added cost with the pair pass's working allotment of 10.57 ms (`w1-128k`'s 11.65 ms headroom, a lower bound, less long range's 1.0 ms and the bodies' idle 0.076 ms, `docs/perf-report.md:139,322-325`), provisional until task 4.5's settled in-app reading with the term decides the allotment and `parametric-bodies` 9.3's live-body reading replaces the bodies share; correct `src/config_ranges.nim:114`'s "settled 128k headroom of 3.75 ms" to cite `w1-128k`'s 11.65 ms
- [ ] 1.6 Read whether Chromium's Dawn requests strict math for WGSL on Metal (Dawn toggles and Chromium's WebGPU device setup); record the answer in `design.md` D4 and in the GPU bit-identity line of the spec
- [ ] 1.7 Group 1 closes with `just happen` and `just check` green

## 2. Shared unit, demand functions and the velocity words

- [ ] 2.1 Write red suite "Every Compressor Answers In The Pair Unit" in `tests/test_balance_core.nim`: one touching neighbour's repulsion at force strength 1 over the reference frame is exactly `u0`; for each demand function, a sweep of its oracle over a grid of configurations inside the ranges finds no inward impulse above the demand and the demand's configuration attains it; verify it fails to compile first, and that a demand evaluated at a non-maximal configuration (the attraction peak moved off the bump's maximum) locally turns it red
- [ ] 2.2 Implement `u0` and demand functions for pair, long range, bodies (per `parametric-bodies` D16), field force and mouse in `src/balance_core.nim`, importing oracles and never `config_ranges`; verify 2.1 passes
- [ ] 2.3 **Oracles first:** write today's convention as oracles, each writer's encode with its substep multiply (pair force, mouse, blast, SPH pressure and velocity blend, bodies, field force, long range) and integrate's single-word decode, in `src/physics_core.nim` and the writers' cores; derive or record as a finding the field-force and long-range per-particle maxima, and fix `k` as the largest the fine word admits and `q_max` as the largest the coarse word admits after SPH's. **Red:** add the fine-word and coarse-word full-crowd assertions at the bottom of `src/config_ranges.nim`, each term naming its constants, and the suites "A Full Crowd Decodes To Its Impulse" (every writer, frame factors 1, 2 and 30) and "Today's Low Bits Move By Less Than The Frame Factor" in `tests/test_physics.nim`; verify the single-word assertion fails the build on today's constants and that the full-crowd suite fails on values against the today-convention oracles (the pair force's once-converted share saturating at frame factor 30, SPH's per-pair adds wrapping). **Green:** move every writer (`forces.wgsl`, `forces-sph.wgsl`, `body-force.wgsl`, `field-force.wgsl`, `lr-force.wgsl`, and their `frameFactor` multiplies in `src/webgpu_compute.nim`) to per-reference-frame accumulation, add the coarse word and the split to SPH, carry the frame factor in an `IntegrationParams` pad slot, decode both words and multiply once in `integrate.wgsl`; verify both suites go green, and that leaving one writer's substep multiply in place, or replacing the split's `>>` by a division, locally turns them red. No range is narrowed
- [ ] 2.4 Group 2 closes with `just happen` and `just check` green

## 3. Long-range unit

- [ ] 3.1 Write red suites "The Pull Does Not Depend On Mesh Size" (impulse 240 and 600 from the clump centre, across `LR_GRID_SIZES`, tolerance the mesh-to-mesh gap of the static solve under the new unit, 0.15% at 240 and 0.10% at 600) and "The Pull Is The Pair Unit Spread By The Green's Function" (reach `LONG_RANGE_REACH_MAX`, 240 from the centre, radii 10, 50 and 150, tolerance the formula gap measured there, 0.7%) and "One Long-Range Ceiling Holds At Every Radius" in `tests/test_long_range_core.nim`, each tolerance and its measured source beside it; verify all three fail on today's code (4.006 ratio; `cellArea` factor; a ceiling moving with the radius), and that `u0 · R` in place of `U(R)` locally turns the second and third red
- [ ] 3.2 Replace `cellArea` by `U(R) = u0 · R² · (a + R) / a²`, `a = √(A_world / (π · x_on))`, in a `long_range_core` force-scale function and write it to `LR_FORCE_SCALE` in `src/webgpu_compute.nim`; verify 3.1 passes and the existing linearity suite stays green
- [ ] 3.3 Write the red `fromVersion < 5` conversion tests in `tests/test_preset.nim` (each grid size, radii 10, 50 and 150, factor `cellArea / U(R)`, zero stays zero, a converted value above the ceiling clamps), then bump `CURRENT_SCHEMA_VERSION` to 5 and add the branch in `src/preset.nim`, with the `x_on` the version-5 unit was defined at recorded there and a static assertion holding it equal to the live onset constant; verify red then green, and that changing the onset constant locally fails the build
- [ ] 3.4 In the same pass as 3.3, amend `openspec/changes/long-range-mesh/specs/long-range-coupling/spec.md` (replace "no schema version and no migration branch is added" with the version-5 conversion this change adds) and its `specs/parameter-range-authority/spec.md` delta (replace the provisional ceiling with the derived one); verify `openspec validate long-range-mesh --strict` passes
- [ ] 3.5 Derive `LONG_RANGE_STRENGTH_MAX` in `src/balance_core.nim` (`MAX_PARTICLES` in one disc at the onset density, one radius past its edge, equals the pair peak edge impulse at that density, one value at every radius under `U(R)`), replace the provisional note in `src/config_ranges.nim` with the derivation and a static assertion; verify the build passes and fails when the assertion's relation is broken locally
- [ ] 3.6 Group 3 closes with `just happen` and `just check` green

## 4. Pressure constants and balance

- [ ] 4.1 Write red suites, at radius 50 on 16 held-out seeds (16 000 particles unless stated), one-sided at `α = 5%` with the D5 margin, in `tests/test_balance_core.nim`, run by a new recipe `just calibrate-balance` that `just check` does not call, except where stated: "A Compressed Crowd Stays Local And Below Its Collapse" (32 aligned D16 bodies at the ceilings for 100 held frames; held peak below the stiffness-zero control's at the same frame; far crowds' mean speed within the margin of a no-body run), "Compression Is Not Remembered" (one self-attracting species; per-seed after-over-fresh neighbour ratio 900 frames after removal, at most `B_r` from 1.3; and a 128 000-particle arm holding the mean ratio at most `1 + t · s / √n`) and "A Settling World Still Settles" (at 128 000 particles and frame factor 1, `mean(L)` at `FRICTION_MIN` at most `B_L`); the 128 000-particle arms and `B_L`'s re-derivation on the calibration seeds run in a second recipe `just calibrate-balance-128k` that neither `just check` nor `just calibrate-balance` calls (about 40 core-hours, design D10); verify the first two fail with `K = 0` (the relaxation suite at both counts), that `K = 1728` locally turns the third red, and that a live stiffness stepped with the body envelope locally turns the far-crowd check red
- [ ] 4.2 Record `x_on` (from 1.2), `K = 540` and `B_L` (from 1.3), `k` and `q_max` (from 2.3) in `src/config_ranges.nim` with one or two lines of conditions each; verify 4.1 passes, and that the build fails when `q_max` is doubled locally
- [ ] 4.3 Wire the pressure: `ρ̄` and the floor computed once per frame from live particle count, radius, world area and pair-law shape in `src/webgpu_compute.nim`; placeholders in `src/shader_config.nim`; the term in `web/shaders/src/forces.wgsl` accumulated apart from the force law's expression, split across the words; the uniform-producer check in `tests/test_sim_registry.nim`; verify `just happen` green, `just calibrate-balance` green (the 16 000-particle arms, frame-factor arms included) and `just calibrate-balance-128k` green (`B_L` re-derived, gate 7's friction-0 check and gate 6's 128 000-particle relaxation arm, about 40 core-hours; it runs again on a change to `K`, the pressure law, the onset, the fine/coarse word split (`k`, `q_max`), or the crowd-density computation's dependence on particle count (`ρ̄`, the floor, the smoothing))
- [ ] 4.4 Rewrite `calibrate-shipped-defaults` fixture C as a world whose crowd rises past the onset at crowding 0, its validity gate and red step 2.5 to match, and remove the `c_hold` constraint so crowding is calibrated by `c_soften` alone, in `openspec/changes/calibrate-shipped-defaults/design.md` and `tasks.md`; verify `openspec validate calibrate-shipped-defaults --strict` passes
- [ ] 4.5 Carry the frame factor to the executor's substep count: `substepCount = max(fluid substeps, ⌈ff / ff_stable⌉)` in `src/webgpu_compute.nim`, with `ff_stable` in `src/config_ranges.nim` beside its conditions; write the red check first in `tests/test_balance_core.nim` ("A Settling World Still Settles" shipped-friction arms at frame factors 2, 10 and 30, a frame factor drawn per frame uniformly from 8 to 16, and one alternating 10 and 13), verify it fails with the trigger absent and with a substep that advances `ff` rather than `ff / n`, then passes; launch the app per CLAUDE.md with Browser MCP connected and read `physics=` and the frame time at 128 000 particles at time scale 0.5 and 5 on a 60 Hz display and with frames held past the 0.05 s cap, read `physics=` on a world settled with the term at `MAX_PARTICLES` and the shipped radius over a window long enough that its last four readings stop rising, record that headroom and the allotment it gives (this reading decides the allotment, and the task landing the term cannot close before it), record the added cost per substep against that allotment and whether a capped frame recovers when its load goes, in `scratchpad/coupling-balance/`; a cost past the allotment returns to the user without narrowing the time-scale range; verify `just calibrate-balance` green
- [ ] 4.6 Group 4 closes with `just happen` and `just check` green

## 5. Cross-coupling probes and docs

- [ ] 5.1 Write red suite "Couplings Are Compared On One Scale" in `tests/test_response_probe.nim` (each probe's impulse at its maximum against a one-frame stepped measurement of its compressor in the binned oracle world; each compressor's relative density where capacity meets demand reported with no bound asserted), then change `longRange.impulseShare` in `src/ui/api/response_probe.nim` to report in `u0` at the reference colony; verify red then green, and that a probe calling its oracle with a halved neighbour sum locally turns it red
- [ ] 5.2 Update `docs/one-world.md` (pressure as part of the pair law, fixed and local), `docs/enforcement.md` (new tiers from the spec, and gates 5–7's tier: held by `just calibrate-balance` and `just calibrate-balance-128k`, both at the same tier, run at change time and not by every `just check`; `just calibrate-balance-128k` reruns on a change to `K`, the pressure law, the onset, the fine/coarse word split (`k`, `q_max`), or the crowd-density computation's dependence on particle count (`ρ̄`, the floor, the smoothing), and that rerun is itself unenforced), the `longRangeStrength` line in `docs/help/35-long-range.md` (at a fixed strength, a larger interaction radius strengthens the pull about as its square), and the crowding help line in `docs/help/12-species.md` to "Crowding shapes clump texture, and the pressure is what bounds collapse"; verify `tests/test_help_content.nim` passes
- [ ] 5.3 Correct the `web/shaders/modules/fixed_point.wgsl` header's claim that 2^16 is far more range than a per-frame impulse needs: state the per-reference-frame convention, the fine and coarse words and the assertions that hold them; correct the `web/shaders/src/forces-sph.wgsl` header's claim that it reads smoothed lagged density, which `integrate.wgsl:76-85` contradicts; verify `just happen` green
- [ ] 5.4 Group 5 closes with `just happen` and `just check` green

## 6. In-app verification

- [ ] 6.1 Agent procedure, Browser MCP connected per CLAUDE.md: at 128 000 particles and 12 species raise Long Range to its maximum for 10 s, then place bodies at Hold 10 for 10 s, then remove them; observe that far colonies keep their motion while the bodies hold, record the held `physics=` figure (a held crowd may exceed the allotment by the user's decision, so it is recorded, not gated), and observe whether the population spreads back within 15 s; then set friction to 0 over a settled self-attracting world and record whether dense crowds shimmer or collapse, and at shipped friction record whether the dense clumps' simmer is visible; record the run in `scratchpad/coupling-balance/in-app__<timestamp>.md`. `parametric-bodies` 9.2 and 9.3 take their measurements after this task
- [ ] 6.2 Agent procedure: at shipped defaults on a 60 Hz display (frame factor 2), compare `physics=` and a screenshot before and after the change on the same seed; observe no visible change in the settled look and a `physics=` difference within the added cost recorded in 1.5
- [ ] 6.3 Group 6 closes with `just happen` and `just check` green
