## 1. Measurement gates

- [ ] 1.1 Write the pressure oracle `pressurePairImpulse` (the square law on `x = ρ/ρ̄`, one symmetric integer per pair) in `src/physics_core.nim`, the uniform crowd density function and a binned stepped oracle world with D16-law bodies in `src/balance_core.nim` (new), with the red suites "Pressure Past The Onset" and "The Force Law Is Untouched Below The Onset" in `tests/test_physics.nim` written first; verify both fail to compile before the oracle exists and pass after, and that the bit-identity suite fails when the term is folded into the force law's `forceMultiplier * invDistance` product locally
- [ ] 1.2 Record a calibration seed set and a disjoint held-out seed set (at least 16 each; species counts 1, 4, 8, 12) in `scratchpad/coupling-balance/`; on the calibration seeds measure the settled crowd density over `ρ̄` at 16 000 and 128 000 particles and radii 50 and 100, running each world until its p99.9 changes less than its frame-to-frame spread; record the distribution and verify `x_on` lies above every mixed-matrix peak
- [ ] 1.3 On the calibration seeds, find the largest `K` at which the ratio of late-window to earlier-window mean speed, self-attracting worlds included, stays within its no-term spread at `FRICTION_MIN` and at the shipped friction; record the runs and the value in `scratchpad/coupling-balance/`
- [ ] 1.4 On the calibration seeds, find the coarsest pressure scale `S_p` that keeps 1.3's criterion and the relaxation of a 32-body hold, and the per-pair saturation `q_max` the full-crowd bound then admits; run the app-scale stacked hold with that saturation and record the held peak; verify the saturation does not reopen the collapse (peak below the stiffness-zero control), and return the design if it does
- [ ] 1.5 Add the pressure word and term to `web/shaders/src/forces.wgsl` behind `K = 0` and launch the app per CLAUDE.md with Browser MCP connected; read `physics=` from `[gpu-profile]` at `MAX_PARTICLES` on a settled shipped world with the word bound and unbound, and on a uniform world at two radii; record the per-neighbour cost, the word's own cost and the term's cost in `scratchpad/coupling-balance/`; the design returns if the settled shipped world's `physics=` exceeds the pair pass's provisional 2.67 ms allotment (3.75 ms headroom, `src/config_ranges.nim:114`, less long range's 1.0 ms, less the bodies' idle 0.066–0.076 ms, `docs/perf-report.md:322-325`), recorded beside the figure as provisional until `parametric-bodies` 9.3's live-body reading replaces the bodies share
- [ ] 1.6 Read whether Chromium's Dawn requests strict math for WGSL on Metal (Dawn toggles and Chromium's WebGPU device setup); record the answer in `design.md` D4 and in the GPU bit-identity line of the spec
- [ ] 1.7 Group 1 closes with `just happen` and `just check` green

## 2. Shared unit and demand functions

- [ ] 2.1 Write red suite "Every Compressor Answers In The Pair Unit" in `tests/test_balance_core.nim`: one touching neighbour at force strength 1 over the reference frame returns exactly 1; each demand agrees with a direct evaluation of its oracle; verify it fails to compile first, and that doubling one demand function locally turns it red
- [ ] 2.2 Implement `u0` and demand functions for pair, long range, bodies (per `parametric-bodies` D16), field force and mouse in `src/balance_core.nim`, importing oracles and never `config_ranges`; verify 2.1 passes
- [ ] 2.3 **Red first:** add the summed full-crowd assertion for the velocity word at the bottom of `src/config_ranges.nim` (every writer's per-particle maximum per reference frame times 2^16 under 2^31 − 1; derive or record as a finding the long-range, field-force and SPH maxima) and the suite "A Full Crowd Decodes To Its Impulse" in `tests/test_physics.nim` (full crowd at contact, `FORCE_STRENGTH_MAX`, frame factor 30, writer oracle encode then integrate oracle decode, within one quantum times the frame factor, with sign); verify the assertion fails the build on today's code (160 000 against 32 767) and the suite fails (saturated or wrapped decode). Then move every writer (`forces.wgsl`, `forces-sph.wgsl`, `body-force.wgsl`, `field-force.wgsl`, `lr-force.wgsl`, and their `frameFactor` multiplies in `src/webgpu_compute.nim`) to per-reference-frame accumulation, carry the frame factor in an `IntegrationParams` pad slot, multiply once in `integrate.wgsl`; verify both go green, and that leaving one writer's substep multiply in place locally turns the suite red. No range is narrowed
- [ ] 2.4 Group 2 closes with `just happen` and `just check` green

## 3. Long-range unit

- [ ] 3.1 Write red suites "The Pull Does Not Depend On Mesh Size" (impulse 240 and 600 from the clump centre, across `LR_GRID_SIZES`) and "The Pull Is The Pair Unit Spread By The Green's Function" (reach `LONG_RANGE_REACH_MAX`, 240 from the centre) in `tests/test_long_range_core.nim`, with the tolerance and its measured source beside them; verify both fail on today's code (4.006 ratio; `cellArea` factor)
- [ ] 3.2 Replace `cellArea` by `u0 · R` in a `long_range_core` force-scale function and write it to `LR_FORCE_SCALE` in `src/webgpu_compute.nim`; verify 3.1 passes and the existing linearity suite stays green
- [ ] 3.3 Write the red `fromVersion < 5` conversion tests in `tests/test_preset.nim` (each grid size, two radii, zero stays zero, a converted value above the ceiling clamps), then bump `CURRENT_SCHEMA_VERSION` to 5 and add the branch in `src/preset.nim`; verify red then green
- [ ] 3.4 In the same pass as 3.3, amend `openspec/changes/long-range-mesh/specs/long-range-coupling/spec.md` (replace "no schema version and no migration branch is added" with the version-5 conversion this change adds) and its `specs/parameter-range-authority/spec.md` delta (replace the provisional ceiling with the derived one); verify `openspec validate long-range-mesh --strict` passes
- [ ] 3.5 Derive `LONG_RANGE_STRENGTH_MAX` in `src/balance_core.nim` (`MAX_PARTICLES` in one disc at the onset density, one radius past its edge, equals the pair peak edge impulse at that density), replace the provisional note in `src/config_ranges.nim` with the derivation and a static assertion; verify the build passes and fails when the assertion's relation is broken locally
- [ ] 3.6 Group 3 closes with `just happen` and `just check` green

## 4. Pressure constants and balance

- [ ] 4.1 Write red suites "A Compressed Crowd Stays Finite And Local" (32 aligned D16 bodies at the ceilings on the held-out seeds at `MAX_PARTICLES`; finite held peak below the stiffness-zero control; far crowds' mean speed within the no-body spread), "Compression Is Not Remembered" (900 frames after removal, mean neighbour count within the spread of fresh settles at the same frame count, including one self-attracting species) and "A Settling World Still Settles" (held-out speed ratio within its no-term spread at `FRICTION_MIN` and shipped friction; verify a Tait law with an onset step locally turns it red) in `tests/test_balance_core.nim`; verify the first two fail with `K = 0`, and that a live stiffness stepped with the body envelope locally turns the far-crowd check red
- [ ] 4.2 Record `x_on` (from 1.2), `K` (from 1.3), `S_p` and `q_max` (from 1.4) in `src/config_ranges.nim` with one or two lines of conditions each; add the full-crowd static assertion for the pressure word; verify 4.1 passes, and that the build fails when `q_max` is doubled locally
- [ ] 4.3 Wire the pressure: `ρ̄` computed once per frame from live particle count, radius and world area in `src/webgpu_compute.nim`; placeholders in `src/shader_config.nim`; the pressure word in the buffer layout; the term in `web/shaders/src/forces.wgsl` accumulated apart from the force law; the decode in `web/shaders/src/integrate.wgsl`; the uniform-producer check in `tests/test_sim_registry.nim`; verify `just happen` green
- [ ] 4.4 Rewrite `calibrate-shipped-defaults` fixture C as a world whose crowd rises past the onset at crowding 0, its validity gate and red step 2.5 to match, and remove the `c_hold` constraint so crowding is calibrated by `c_soften` alone, in `openspec/changes/calibrate-shipped-defaults/design.md` and `tasks.md`; verify `openspec validate calibrate-shipped-defaults --strict` passes
- [ ] 4.5 Group 4 closes with `just happen` and `just check` green

## 5. Cross-coupling probes and docs

- [ ] 5.1 Write red suite "Couplings Are Compared On One Scale" in `tests/test_response_probe.nim` (each probe's impulse at its maximum against its demand function evaluated directly; each compressor's relative density where capacity meets demand reported with no bound asserted, per the relative ceiling), then change `longRange.impulseShare` in `src/ui/api/response_probe.nim` to report in `u0` at the reference colony; verify red then green, and that doubling a demand function locally turns it red
- [ ] 5.2 Update `docs/one-world.md` (pressure as part of the pair law, fixed and local), `docs/enforcement.md` (new tiers from the spec), the long-range help line in `docs/help/`, and the crowding help line in `docs/help/12-species.md` to "Crowding shapes clump texture, and the pressure is what bounds collapse"; verify `tests/test_help_content.nim` passes
- [ ] 5.3 Correct the `web/shaders/modules/fixed_point.wgsl` header's claim that 2^16 is far more range than a per-frame impulse needs: state the per-reference-frame convention and the full-crowd assertion that holds it, and name the pressure word's scale; correct the `web/shaders/src/forces-sph.wgsl` header's claim that it reads smoothed lagged density, which `integrate.wgsl:76-85` contradicts; verify `just happen` green
- [ ] 5.4 Group 5 closes with `just happen` and `just check` green

## 6. In-app verification

- [ ] 6.1 Agent procedure, Browser MCP connected per CLAUDE.md: at 128 000 particles and 12 species raise Long Range to its maximum for 10 s, then place bodies at Hold 10 for 10 s, then remove them; observe that far colonies keep their motion while the bodies hold, record the held `physics=` figure (a held crowd may exceed the allotment by the user's decision, so it is recorded, not gated), and observe that the population spreads back within 15 s; then set friction to 0 over a settled self-attracting world and record whether dense crowds shimmer below the soft cap or collapse; record the run in `scratchpad/coupling-balance/in-app__<timestamp>.md`. `parametric-bodies` 9.2 and 9.3 take their measurements after this task
- [ ] 6.2 Agent procedure: at shipped defaults, compare `physics=` and a screenshot before and after the change on the same seed; observe no visible change in the settled look and a `physics=` difference within the cost recorded in 1.5
- [ ] 6.3 Group 6 closes with `just happen` and `just check` green
